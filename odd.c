#include<stdio.h>
#includ<conio.h.
int main()
{
int low,flag,i,flag;
printf(" enter the number between two intervals:");
scanf("%d %d",&low,&high);
while(low<high)
{
flag=0;
for(i=2;i<=low/2;++i)
{
if(low%i==0)
{
flag==0;
printf("%d",low);
++low;
}
return 0;
}
